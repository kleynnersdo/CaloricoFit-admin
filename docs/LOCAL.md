# Calórico Fit — Setup local

| Servicio | Puerto | URL |
|----------|--------|-----|
| Vite dev | **3020** | http://localhost:3020 |

Puertos 3000–3010 suelen estar ocupados por otras apps en esta máquina.

```bash
npm install
npm run dev
```

Variables: ver `.env.example` / `.env`.

## DB smoke (Postgres local)

Ver [LOCAL_DB.md](./LOCAL_DB.md) — Adminer en http://localhost:8092 · puerto **15433**.
